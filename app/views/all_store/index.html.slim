.row.top-row
  H1 List of all the stores

.container
  .table
    .tablefont
      table.table.table-striped
        thead
          tr
            th Store Id
            th Store Address
        tbody
          - AllStore.all.each do |row|
            tr
              td = link_to row.id, store_path(row.id)
              td = row.address
  button = link_to "Home", root_path